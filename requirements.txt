# Núcleo do pipeline (processamento local, sem Java/Spark)
duckdb==1.5.4
polars==1.42.1
pyarrow==24.0.0
pandas==3.0.3
pyyaml==6.0.3

# Ingestão real da Base dos Dados via BigQuery (usado por scripts/bq_download.py)
google-cloud-bigquery==3.42.1
google-cloud-bigquery-storage==2.39.0
db-dtypes==1.7.0

# Publicação na nuvem (data lake em GCS) — opcional
google-cloud-storage>=2.0.0

# Modo cloud-native (lake lido/gravado direto no GCS via fsspec)
gcsfs>=2024.1.0
